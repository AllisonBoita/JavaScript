// IF e ELSE (Se, Se não)

let hour = 12

if (hour > 12) {
  console.log('boa tarde')
} else {
  console.log('bom dia')
}

// Validado de cima pra baixo! executa o IF, se for falso avança para o ELSE IF. Se for verdadeiro, para por ali.

// Utilizando o ELSE IF

/* let hour = 18

if (hour <= 12) {
  console.log('boa dia')
} else if (hour > 12 && hour <= 18) {
  console.log('boa tarde')
} else {
  console.log('boa noite')
} */

// Variação de formato de IF e ELSE
